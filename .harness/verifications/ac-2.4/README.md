# AC-2.4 verification bundle

Proves: brief token limit (2,000) and exact truncation marker (`<!-- truncated -->`)
are specified, and the truncation rule is structurally well-formed.

Token = whitespace-separated word (approximation documented in `_shared/lib.sh`).
