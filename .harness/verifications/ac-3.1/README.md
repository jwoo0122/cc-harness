# AC-3.1 — Iteration selection & name validation (Phase 0a)

Proves: Phase 0a exists; `.iteration-*/` glob enumeration; canonical regex stated; 1/N/0-candidate branches; hard-error abort; Python regex oracle cross-checks valid/invalid corpus.
