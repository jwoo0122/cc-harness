# AC-C.1 — md_section helper, fence-aware

Proves: md_section extracts target `## Heading` through next `## ` heading; in-fence `## ` is preserved as content (backtick + tilde + info-string variants).
